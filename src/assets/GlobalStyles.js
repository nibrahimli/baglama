import { StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#eccd9c',
    alignItems: 'center',
    justifyContent: 'center',
  },
});